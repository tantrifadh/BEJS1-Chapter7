const request = require("supertest");
const app = require("../app");
const {component} = require("../models");

jest.mock("../models");

const mockComponent = require("../")

//test case
DESCRIBE('COMPONENT controller', () => {
    let token;
    //clear all mock first test
    beforeAll(async () => {
        
    })
})